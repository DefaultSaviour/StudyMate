<?xml version="1.0" encoding="utf-8"?>
<!-- Filled circle for the onboarding progress dots; tinted at runtime
     (gold for the active page, faded gold for inactive). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/white" />
    <size
        android:width="9dp"
        android:height="9dp" />
</shape>
